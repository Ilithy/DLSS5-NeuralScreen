# Рабочая папка с диагностикой и тестами — не коммитится
_work/

# Python
__pycache__/
*.pyc

# Сборочные артефакты воркера (собирается native/build-host.bat)
*.obj
native/nvngx.dll

# Редистрибутив NVIDIA: 165 МБ, превышает лимит файла GitHub (100 МБ).
# Как его получить — см. README.md, раздел «Требования».
native/nvngx_dlssnr.dll

# Опциональный локальный Python-рантайм рядом с программой
runtime/

# Лог программы (pythonw-запуск без консоли)
NeuralScreen.log

# Записи с Insert
recordings/

# Релизные архивы в корне (собираются при релизе заново)
*.zip

# Скриншоты, снятые кнопкой в меню
screenshots/

# Резервные копии от патч-скриптов
*.bak-*

# Offscreen-рендеры меню для просмотра вёрстки
_menu*.png
*.local-backup
release-package.html
# Dev-скрипты и личные файлы — не для репозитория
_measure_*.py
_probe_*.py
_render_*.py
test_bake_menu.py
requirements.txt
